/** @format */

// It would not be the worst if these were released on github, but I'd rather be on the safe side
declare const consts: {
    canonicalURL: string
    apiURL: string
    githubAPIKey: string
}
export default consts
